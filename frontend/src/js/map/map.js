// TODO: Inicializar Leaflet, centrar Bogotá
export function initMap() {
  // const map = L.map('map').setView([4.7110, -74.0721], 12);
  // L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png').addTo(map);
}