// TODO: Formulario crear reporte + geolocalización
export function initReportForm() { }
export function getCurrentLocation() {
  // return new Promise((resolve, reject) => navigator.geolocation.getCurrentPosition(resolve, reject));
}